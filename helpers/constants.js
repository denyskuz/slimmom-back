const defaultPageSize = 20;
const defaultPage = 1;
const cookieName = 'slimmoms';
const expiresCookieTime = 10; // minutes
const expiresAccessTime = '10m';
const expiresRefreshTime = '1d';

module.exports = {
  defaultPageSize,
  defaultPage,
  cookieName,
  expiresCookieTime,
  expiresAccessTime,
  expiresRefreshTime,
};
